{
  "name": "server-sent-events",
  "version": "1.0.0",
  "description": "poc implementation for streaming the llm response and sse is used by the openai",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
